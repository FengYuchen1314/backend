# 二次开发进度

更新日期：2026-09-06。本次仅汇总并上传已有进度，不继续功能开发或部署 VPS。当前为开发分支检查点，尚未完成全部需求验收，不作为生产发布。

## 仓库与版本

主要修改沿用原项目的配置文件、入站、Host、Node 和订阅结构。前后端及 Agent 位于以下三个 fork；本轮不合并主分支，也不更新 `xboard-dev` 发布标签。

| 仓库                                                                             | 当前开发分支             | 本轮检查点                                                          |
| -------------------------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------- |
| [Backend](https://github.com/FengYuchen1314/backend/tree/wip/shared-443-backend) | `wip/shared-443-backend` | 最新代码 `c5b9d397a7df46e766c61aa39c62e672e56b96cc`；本次只更新文档 |
| [Frontend](https://github.com/FengYuchen1314/frontend/tree/wip/shared-443-ui)    | `wip/shared-443-ui`      | 最新代码 `eb8f1790ec080787091e4c00cdaf05cdaf7b04e4`；本次只更新文档 |
| [Node](https://github.com/FengYuchen1314/node/tree/wip/shared-443-node)          | `wip/shared-443-node`    | `0e8ae34f`，本轮无修改                                              |

上述代码提交均已核对 GitHub 远端 SHA。本次再提交进度说明及 HeroUI 迁移范围清单，文档提交不改变这些已测试代码版本。安装凭据、私钥、数据库备份和私有测试环境文件不随代码上传。

## 本轮 bug 修复检查点

以下改动已通过前后端 Actions 检查；Backend 的真实数据库回归已通过，但这批前端修复尚未部署到 VPS 完成界面验收，不能与下文旧版镜像实测混为一谈。

| 范围           | 本轮改动                                                                                                                                                                  | 验证边界                                                                                                                |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 表单草稿       | 移除响应头、备注提交前的延迟副本；立即保存使用最新输入。按记录 UUID 隔离草稿；修复标签默认值、自建标签保留及数字 0 的显示。                                               | 事件逻辑和接线回归通过；快速保存、失败重试和切换记录仍待真实浏览器验收。                                                |
| 虚拟列表       | 五处列表改用项目已安装的 react-virtuoso，保留虚拟化和选择逻辑；修复平铺入站列表包装层导致的 0 高度；移除节点表格渲染期间的 ref 读写。                                     | 独立浏览器 SSR/CSS 探针证明平铺列表视口从 0px 恢复为 342px；不代表完整页面的滚动、选择、加载更多或移动端交互已验收。    |
| 会话与异步请求 | 为请求和每次 mutation 绑定会话代次，拦截旧响应、错误、异步解析及回调；补齐拓扑、NodeEdge、账单和 Vault evaluation 的独立调用路径。切换 token 清理缓存，查询透传取消信号。 | 真实 Axios 适配器、MutationObserver 和 hook SSR 回归通过；尚非多账号浏览器端到端验收。                                  |
| SSH 密钥库     | 锁定、注销和替换 token 后使旧解锁、读取和恢复操作失效；清除内存密钥，串行持久化写入，保留原加密算法及参数。                                                               | 12 项真实加密、内存数据库替身回归通过；真实 IndexedDB 生命周期未验收，DB 删除错误处理和多步骤恢复非原子性仍在后续范围。 |
| Passkey 登录   | 保护 options → 认证器 → verify 整个流程；拒绝缓存的失败挑战、旧 assertion 和迟到 token 提交，避免全局取消误伤其他认证器请求。                                             | 10 项事件/流程回归通过，不是硬件认证器验收。卸载后原生提示可能仍存在，但旧结果不能提交。                                |
| 节点停止       | 按实际入站协议选择 Xray/Mieru 停止接口；异常混合选择尝试停止两者，全部确认后才报告成功。                                                                                  | 6 项停机单测通过；本批尚未部署真实 Agent 联测。                                                                         |
| 节点配置原子性 | 单节点和批量替换纳入 Prisma/CLS/Kysely 同一事务，先锁节点，再替换关联；失败回滚，提交后才排队和通知。                                                                     | 8 项事务单测及 Actions 真实 PostgreSQL 回归通过，覆盖唯一键/外键失败回滚、队列前完整状态及 8 次并发替换。               |

检查与构建结果：

- Frontend `eb8f1790` 的 [Actions CI](https://github.com/FengYuchen1314/frontend/actions/runs/33976780218) 已成功：53 项原有/表单/列表测试和 73 项会话/Vault/Passkey 测试，共 126 项全部通过；TypeScript、全仓 lint 和应用编译通过。本地对应检查也已通过。
- 本地测试借用 Backend tsx 4.21.0；上述前端 CI 已安装并验证 lockfile 固定的 tsx 4.23.13，区分本地运行环境与 Actions 干净安装结果。
- Backend `c5b9d397` 的 [Actions CI](https://github.com/FengYuchen1314/backend/actions/runs/33975968368) 已成功，包含真实 PostgreSQL 并发与回滚测试、真实客户端订阅回归、应用编译及 OpenAPI/依赖注入检查。本地默认测试 readiness 3/3、业务 95 项通过，2 项 Linux 安装测试仅在 Windows 跳过，未把本地跳过当成通过。
- 精确 Backend `c5b9d397` + Frontend `eb8f1790` 的 [配套镜像任务](https://github.com/FengYuchen1314/backend/actions/runs/33976781168) 汇总时仍在执行：后端验证和前端编译完成，镜像构建/发布进行中。它不是默认 `xboard-dev` 前端的自动配对任务；最终状态以该次运行结果为准。
- 应用仅在 GitHub Actions 编译。新代码尚未升级 VPS 隔离面板；备份、版本核对及配置保留检查脚本已准备但未执行，因此不宣称本批修复已经通过 VPS 验收。

### HeroUI 和前端逻辑重写

目前只完成范围评估和代码盘点，尚未安装 HeroUI、迁移页面或替换 Mantine。盘点覆盖 30 个真实页面入口、404/error fallback、55 个弹窗注册项及动态弹窗/抽屉，不能按局部换肤算作完成。

详细清单见 Frontend 的 [HeroUI 迁移范围与验收清单](https://github.com/FengYuchen1314/frontend/blob/wip/shared-443-ui/docs/heroui-migration-scope.md)；最新修复证据见 [会话、密钥库及列表回归修复](https://github.com/FengYuchen1314/frontend/blob/wip/shared-443-ui/docs/session-and-vault-bugfix-checkpoint.md)。

后续迁移应保留原项目目录、路由和全部管理功能，先拆出表单草稿、会话、查询和编排逻辑，再使用 HeroUI 原生组合方式替换界面组件。表格分页、筛选、排序、跨页选择、批量操作和持久化偏好，以及图表、Monaco、终端、虚拟列表、WebAuthn 和加密核心均在保留范围内。本次不启动这项重写。

## 原始需求对照

| 需求                               | 已有进度与证据                                                                                                                                        | 尚未完成                                                                                                                           |
| ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 面板内一键更新                     | 已有按钮、状态 API 和独立 updater；实现固定镜像来源、升级兼容性检查、健康检查及回退逻辑。                                                             | 完整部署环境中的点击更新、故障回退与恢复验收。                                                                                     |
| Agent 文件全部从面板拉取           | 原版安装命令已在空镜像、无法访问镜像仓库的隔离环境中执行成功。Agent、HAProxy、Caddy 镜像均从面板获取并校验，之后真实 Agent 可用。                     | 不把已通过的公网安装案例扩大为所有服务器类型、平台及升级路径均已验收。                                                             |
| 限定创建协议并保留导入             | 已有两种 VLESS、加密 AnyTLS + ShadowTLS、Mieru TCP、SOCKS5 模板及受管过滤；保留外部导入。AnyTLS 已通过真实面板、Agent 和 Mihomo TCP 全链路。          | 全部协议的实际连接及组合回归，尤其混合 VLESS 与 AnyTLS。                                                                           |
| 服务器类型和协议提示               | 安装命令生成前选择公网、专线或家宽；真实界面确认专线仅列 Mieru、家宽仅列 SOCKS5，公网可选 AnyTLS。已有 SOCKS5 风险提示。                              | 最终按截图核对所有提示与选择规则；当前公网模板不含 Mieru，需复核这一额外限制是否符合需求。                                         |
| 多节点共用 443、独立 SNI、网站反代 | 已实现混合 SNI 路由、冲突校验、反代管理、版本冲突保护和运行时回退。真实 AnyTLS 已经共享 443 入口传输；独立边缘测试验证网站 HTTPS 和 308 跳转。        | 两种 VLESS、AnyTLS、网站在同一服务器同时传输的完整验收；真实公网证书签发。独立边缘测试的代理接收端和私有测试 CA 不能替代这些验证。 |
| 七地区各三组非 Cloudflare 域名池   | 现有 21 个候选域名已从提供的单台大陆 VPS 完成两次探测，未观察到 Cloudflare CDN 信号；Cloudflare 负面对照被拒绝。后端和 Agent 已有多层排除与实时校验。 | 候选扩充为每地区三组独立池，完成探测证据接入及全路径复核。单一大陆网络的历史结果不能证明全国可用，也不能永久保证域名不切换 CDN。   |
| Mieru 国内入口与 IX 映射           | 一对一和手动 IX 端口模式已实现；真实浏览器创建、保存、重开、切换模式、无效端口拦截及面板重启保留均通过。Agent 的 Mieru 原生生命周期和计费另有实测。   | 国内入口实际端口转发与这套表单的联合端到端验收；表单不会自动修改外部路由器。                                                       |
| 图形化链式代理与负载均衡           | 已有图形编辑、保存/发布、版本冲突处理、物理服务器回环校验和 Mihomo 订阅编译。原生客户端已验证 SOCKS5 链、多入口单出口及轮询场景。                     | 多台真实服务器之间、不同协议组合的完整流量验证。现有容器回环测试不能替代跨物理服务器验收。                                         |

## 此前已完成的镜像与 VPS 实测

应用及原生程序均由 GitHub Actions 编译，未在本机或 VPS 编译。最新已验收面板镜像由 Backend `0b5125d12d4ddbaeacdd3d4a6fb93f2b9be158ce` 和 Frontend `1addfa954455333a236abb68d60575e6ed773540` 配套构建：

`ghcr.io/fengyuchen1314/backend@sha256:76f018cab44b52ac110e7fcd57d414362e0ab6f3825f53dbfb6546c9d729ef00`

- [配套镜像构建](https://github.com/FengYuchen1314/backend/actions/runs/33964860911)、[前端 CI](https://github.com/FengYuchen1314/frontend/actions/runs/33964516594) 和 [Agent 镜像构建](https://github.com/FengYuchen1314/node/actions/runs/33960252377) 均成功。前端这一版本通过 42 项测试、类型检查、改动文件 lint 和编译。
- 受管 AnyTLS 从原版安装命令、面板创建、真实 Mihomo 订阅到 TCP 传输均通过，内层证书固定校验与外层证书验证保持开启。
- 957 字节原始流量按 0.5 倍率记为 478 字节。Agent 容器实际重建后，状态卷、计费游标和累计值保留；新增请求后累计为 1,911 字节、计费 955 字节，重复轮询没有再次扣费。Node 倍率、历史流量和用户 API 也做了核对。
- 浏览器验证了 AnyTLS 创建、缺少字段和端口冲突拦截、配置结构校验、无效版本拒绝，以及编辑/撤销后的保存按钮状态。没有保存负面测试草稿。
- 面板升级前备份数据库；原有 2 个编排、5 个完整配置和 4 个 Host 经深度比较保持不变。界面测试仅新增 1 个独立 AnyTLS 配置，最终为 6 个配置。

这些旧版镜像的检查仍不覆盖 UDP、突然断电下绝对无损的计费、所有协议组合或生产环境长期运行。此前前端全仓 lint 的 15 个错误和 11 个警告已修复，本地及最新前端 CI 均通过，但新的 UI 改动尚未重新部署验收。

## 测试环境与后续工作

测试始终使用隔离容器和专用目录。已结束的本轮安装测试容器、网络和专属数据卷均已清理，私有验收证据保留。现有 PDF、MMW 和代理服务未删除；最近验收时原有容器 ID 保持不变，PDF HTTP 返回 200。大陆探测未修改已有服务、端口或防火墙。

本次只收尾已有修改、汇总和上传进度，不继续功能开发。恢复开发时先确认精确配套镜像成功，备份并升级隔离面板，进行快速保存、长列表交互和会话/Vault 浏览器回归，再继续前端逻辑重写和 HeroUI 迁移。原需求中的混合协议共享 443、域名池、一键更新和跨物理服务器编排仍需逐项验收，不能因 UI 迁移遗漏。

详细记录见 [AnyTLS 受管创建](docs/anytls-managed-creation.md)、[累计计费](docs/anytls-cumulative-accounting.md)、[面板升级和数据保留](docs/paired-wip-acceptance.md)、[面板文件分发](docs/panel-only-node-artifacts.md)、[订阅编排](docs/subscription-topology-publication.md)、[更新器边界](docs/updater-sidecar-contract.md)。早期文档中的未完成描述应结合后续带版本的验收记录阅读。
